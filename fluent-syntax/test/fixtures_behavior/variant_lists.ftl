key =
    { SELECTOR() ->
        *[one]
            {
                *[one] Foo
            }
    }

-term =
    { SELECTOR() ->
        *[one]
            {
                *[one] Foo
            }
    }
